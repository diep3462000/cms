generator:
  class: tmcTwitterBootstrapDoctrineGenerator
  param:
    model_class:           LogPayment
    theme:                 tmcTwitterBootstrap
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          log_payment
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:  ~
      list:
        sort: [created_at, desc]
        display: [userid, seria, pin_card, message, providerId,status, created_at, updated_at]
        actions: {}
        object_actions: {}
        batch_actions: {}
      filter:
        display: [userid, seria, providerId, status]
      form:    ~
      edit:    ~
      new:     ~
